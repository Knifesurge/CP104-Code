"""
------------------------------------------------------------------------
[program description]
------------------------------------------------------------------------
Author: Nicolas Mills
ID:     180856100
Email:  mill6100@mylaurier.ca
__updated__ = 2018-11-28
------------------------------------------------------------------------
"""
alphabet = "abcdefghijklmnopqrstuvwxyz"
vignereTable = [[char for char in alphabet] for char2 in alphabet]
for line in vignereTable:
    print(line)
    
filename = "mp4in.txt"
fh = open(filename, "r")
fh_out = open("mp4in_cipher.txt", "w")

key = input("Enter key: ")

def encode(fh, fh_out, key):
    """
    Encodes a file by a Vignere Table given a key
    """
    fh.seek(0)
    file_contents = fh.readlines()
    """ 
    for i in range(len(file_contents)):
        for j in range(len(file_contents[i])):
            # Get char from file
            char = file_contents[i][j]
            # Loop over alphabet to get char's place in alphabet
            for k in range(len(alphabet)):
                # Find char placement in alphabet
                if char == alphabet[k]:
                    for j in range(len(alphabet)):
                        if 
    """
    for line in file_contents:
        # Get individual words
        words = line.split(" ")
        for word in words:
            for i in range(len(word)):
                
    return

encode(fh, fh_out, key)